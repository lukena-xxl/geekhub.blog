{% extends 'base.html.twig' %}

{% block title %}Добавление публикации{% endblock %}

{% block body %}
    <h1>Добавление публикации</h1>
    <div class="mt-3">
        {{ form_start(form_add, {'method': 'POST', 'action': path('article_add'), 'attr': {'style': 'max-width:700px'}}) }}
            {{ form_widget(form_add) }}
        {{ form_end(form_add) }}
    </div>
{% endblock %}
